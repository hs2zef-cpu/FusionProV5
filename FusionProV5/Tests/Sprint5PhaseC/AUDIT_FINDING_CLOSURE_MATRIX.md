# Sprint 5 Phase C.2 audit-finding closure matrix

| Finding | Code correction | Direct MQL evidence | Queue MQL evidence | Reference evidence | Status |
|---|---|---|---|---|---|
| MAJOR-1 Ledger authority integrity | Removed the enum/header/single-record result boundary. The coordinator reads and validates the complete ordered header/index/record snapshot, derives membership with the frozen evaluator, invokes the frozen-shaped expected-state acceptance operation, and verifies the complete post-state. | Valid NEW and DUPLICATE controls; malformed header/index/linkage/record/binding cases; event/proposal/post-state corruption cases. | BUY, SELL, duplicate ingress, WAIT, BLOCKED, and two-request paths all dispatch through `ProcessIngress`. | `ledger_malformed` plus valid/duplicate/no-entry flows. | **CLOSED** |
| MAJOR-2 Request Sequence authority integrity | Replaced result-only reservation with complete expected authority/index, exact frozen reservation proposal, event-bound owner operation, and independently validated resulting authority/index. | Valid NEW and `EXISTING_IDEMPOTENT`; corrupt authority/index; fabricated result; zero revision; empty digest; wrong correlation/sequence; changed sequence; duplicate sequence; stale operation. | BUY, SELL, duplicate, and two-request paths use the same authoritative Sequence adapter. | `sequence_malformed`, duplicate ingress, and two requests. | **CLOSED** |
| MAJOR-3 Request progression validation | Added an injected V5 `ValidatePhaseTransition` adapter and one canonical full-request preservation comparator that allows only state, phase, and authoritative change time to differ. | BUY→SELL, SELL→BUY, volume, price, specification, Risk ID, account mode, Basket, namespace, fence, attempt, idempotency, normalization, expiry, terminal state, wrong target state, wrong phase, and lifecycle denial. | BUY, SELL, and two-request progression events dispatch through `MaterializeAndProgress`. | `progression` and `direction_reversal`. | **CLOSED** |
| MAJOR-4 Core queue→coordinator dispatch | Replaced the shallow selector with a typed, non-authoritative dispatcher for all six public event kinds and the complete ingress→materialization→admission→fake-broker/reconciliation path. | Every handler retains direct actual-function controls. | Full BUY/SELL, duplicate, WAIT, BLOCKED, two requests, progression, admission, duplicate admission, takeover, crash boundaries, reconciliation, and acknowledgement. | All 22 scenarios use the classified orchestration model; BUY/SELL use typed queue-equivalent dispatch stages. | **CLOSED** |

No finding is closed by documentation alone. These are Phase C.2 development self-verification statuses; the independent final orchestration re-audit has not yet been performed.
